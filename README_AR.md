# Tawal Academy - الواجهة الخلفية (Backend)

## 📋 المحتويات
- [نظرة عامة](#نظرة-عامة)
- [المتطلبات](#المتطلبات)
- [التثبيت والتشغيل](#التثبيت-والتشغيل)
- [الـ API Endpoints](#الـ-api-endpoints)
- [كيفية الاستخدام](#كيفية-الاستخدام)
- [رفع على GitHub](#رفع-على-github)
- [النشر على الإنترنت](#النشر-على-الإنترنت)

---

## 📖 نظرة عامة

هذه الواجهة الخلفية توفر:
- **إدارة الطلاب**: تسجيل وحفظ بيانات الطلاب
- **تتبع النتائج**: حفظ ومتابعة درجات الاختبارات
- **الإحصائيات**: حساب متوسط الدرجات وأفضل النتائج
- **سجلات الدخول**: تتبع متى يدخل ويخرج الطالب
- **لوحة الإدارة**: عرض إحصائيات شاملة عن جميع الطلاب

---

## 🔧 المتطلبات

- **Node.js** (الإصدار 14 أو أحدث)
  - تحميل من: https://nodejs.org/

---

## 🚀 التثبيت والتشغيل

### الخطوة 1: تثبيت المكتبات
```bash
npm install
```

### الخطوة 2: تشغيل الخادم
```bash
node server.js
```

ستظهر رسالة مثل:
```
✓ الخادم يعمل على: http://localhost:3001
✓ API متاح على: http://localhost:3001/api
```

### الخطوة 3: اختبار الخادم
افتح المتصفح وادخل:
```
http://localhost:3001/api/health
```

يجب أن تظهر:
```json
{
  "status": "OK",
  "message": "الخادم يعمل بشكل صحيح"
}
```

---

## 📡 الـ API Endpoints

### 1️⃣ تسجيل طالب جديد
**الطلب:**
```
POST http://localhost:3001/api/students/register
Content-Type: application/json

{
  "name": "أحمد محمد",
  "email": "ahmed@example.com"
}
```

**الرد:**
```json
{
  "id": 1,
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "message": "تم التسجيل بنجاح"
}
```

---

### 2️⃣ الحصول على بيانات الطالب
**الطلب:**
```
GET http://localhost:3001/api/students/1
```

**الرد:**
```json
{
  "id": 1,
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "createdAt": "2025-11-08 12:30:45"
}
```

---

### 3️⃣ حفظ نتيجة اختبار
**الطلب:**
```
POST http://localhost:3001/api/quiz-results
Content-Type: application/json

{
  "studentId": 1,
  "quizName": "GIS Networks",
  "score": 85,
  "totalQuestions": 10,
  "correctAnswers": 8
}
```

**الرد:**
```json
{
  "id": 1,
  "message": "تم حفظ النتيجة بنجاح"
}
```

---

### 4️⃣ جلب نتائج الطالب
**الطلب:**
```
GET http://localhost:3001/api/students/1/results
```

**الرد:**
```json
[
  {
    "id": 1,
    "studentId": 1,
    "quizName": "GIS Networks",
    "score": 85,
    "totalQuestions": 10,
    "correctAnswers": 8,
    "completedAt": "2025-11-08 13:00:00"
  }
]
```

---

### 5️⃣ جلب إحصائيات الطالب
**الطلب:**
```
GET http://localhost:3001/api/students/1/stats
```

**الرد:**
```json
{
  "totalQuizzes": 3,
  "averageScore": 82,
  "bestScore": 90,
  "totalCorrect": 24
}
```

---

### 6️⃣ تسجيل دخول الطالب
**الطلب:**
```
POST http://localhost:3001/api/login
Content-Type: application/json

{
  "studentId": 1
}
```

**الرد:**
```json
{
  "logId": 1,
  "message": "تم تسجيل الدخول"
}
```

---

### 7️⃣ تسجيل خروج الطالب
**الطلب:**
```
POST http://localhost:3001/api/logout
Content-Type: application/json

{
  "logId": 1
}
```

**الرد:**
```json
{
  "message": "تم تسجيل الخروج"
}
```

---

### 8️⃣ جلب جميع الطلاب (للإدارة)
**الطلب:**
```
GET http://localhost:3001/api/admin/students
```

**الرد:**
```json
[
  {
    "id": 1,
    "name": "أحمد محمد",
    "email": "ahmed@example.com",
    "createdAt": "2025-11-08 12:30:45"
  },
  {
    "id": 2,
    "name": "فاطمة علي",
    "email": "fatima@example.com",
    "createdAt": "2025-11-08 12:35:20"
  }
]
```

---

### 9️⃣ جلب الإحصائيات العامة (للإدارة)
**الطلب:**
```
GET http://localhost:3001/api/admin/stats
```

**الرد:**
```json
{
  "totalStudents": 25,
  "totalQuizzes": 45,
  "averageScore": 78
}
```

---

### 🔟 جلب سجلات الدخول (للإدارة)
**الطلب:**
```
GET http://localhost:3001/api/admin/login-logs
```

**الرد:**
```json
[
  {
    "id": 1,
    "name": "أحمد محمد",
    "email": "ahmed@example.com",
    "loginTime": "2025-11-08 14:00:00",
    "logoutTime": "2025-11-08 14:45:00"
  }
]
```

---

## 💡 كيفية الاستخدام

### مع المنصة الأصلية

1. **في صفحة quiz.html (صفحة الاختبار)**، أضيفي هذا الكود قبل `</body>`:

```javascript
<script>
// متغير لحفظ معرف الطالب
let studentId = localStorage.getItem('studentId');

// إذا لم يكن هناك طالب مسجل، سجليه
if (!studentId) {
  const name = prompt('ما اسمك؟');
  const email = prompt('ما بريدك الإلكتروني؟');
  
  fetch('http://localhost:3001/api/students/register', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ name, email })
  })
  .then(res => res.json())
  .then(data => {
    studentId = data.id;
    localStorage.setItem('studentId', studentId);
    alert('تم التسجيل بنجاح!');
  });
}

// عند الانتهاء من الاختبار، احفظي النتيجة
function saveQuizResult(quizName, score, totalQuestions, correctAnswers) {
  fetch('http://localhost:3001/api/quiz-results', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      studentId,
      quizName,
      score,
      totalQuestions,
      correctAnswers
    })
  })
  .then(res => res.json())
  .then(data => console.log('✓ تم حفظ النتيجة:', data));
}
</script>
```

---

## 📤 رفع على GitHub

### الخطوة 1: إنشاء حساب GitHub
- اذهبي إلى: https://github.com/signup
- أنشئي حساباً جديداً

### الخطوة 2: إنشاء مستودع جديد
1. اضغطي على `+` في الزاوية العلوية اليمنى
2. اختاري `New repository`
3. أدخلي الاسم: `tawal-academy-backend`
4. اختاري `Public`
5. اضغطي `Create repository`

### الخطوة 3: رفع الملفات
```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit - Tawal Academy Backend"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/tawal-academy-backend.git
git push -u origin main
```

---

## 🌍 النشر على الإنترنت

### الخيار 1: استخدام Heroku (مجاني)

1. **تثبيت Heroku CLI**
   - من: https://devcenter.heroku.com/articles/heroku-cli

2. **إنشاء حساب Heroku**
   - من: https://www.heroku.com/

3. **نشر التطبيق**
   ```bash
   heroku login
   heroku create tawal-academy-backend
   git push heroku main
   ```

4. **الرابط النهائي**
   ```
   https://tawal-academy-backend.herokuapp.com
   ```

### الخيار 2: استخدام Railway (سهل جداً)

1. **اذهبي إلى**: https://railway.app
2. **اضغطي**: `Deploy from GitHub`
3. **اختاري المستودع**
4. **اضغطي**: `Deploy`

### الخيار 3: استخدام Render

1. **اذهبي إلى**: https://render.com
2. **اضغطي**: `New Web Service`
3. **اختاري المستودع**
4. **اضغطي**: `Create Web Service`

---

## 📝 ملاحظات مهمة

- **قاعدة البيانات**: يتم حفظها في ملف `tawal_academy.db` محلياً
- **CORS**: مفعل للسماح بالطلبات من أي موقع
- **الأمان**: للإنتاج، أضيفي مصادقة وتشفير

---

## ❓ أسئلة شائعة

**س: كيف أستخدم الواجهة الخلفية مع المنصة الأصلية؟**
ج: أضيفي كود JavaScript في صفحات المنصة لإرسال البيانات إلى الخادم.

**س: هل يمكن استخدام قاعدة بيانات أخرى؟**
ج: نعم، يمكن استبدال SQLite بـ MySQL أو PostgreSQL.

**س: كيف أحمي الـ API من الوصول غير المصرح؟**
ج: أضيفي مصادقة باستخدام JWT tokens.

---

## 📞 الدعم

إذا واجهتِ أي مشاكل، تأكدي من:
1. تثبيت Node.js بشكل صحيح
2. تشغيل الخادم بـ `node server.js`
3. فتح المنفذ 3001 في جدار الحماية

---

**تم الإنشاء بواسطة**: Manus AI
**التاريخ**: 2025-11-08
