# 📖 دليل التثبيت والنشر الكامل - Tawal Academy Backend

---

## 🎯 المحتويات
1. [تحميل الملفات](#1-تحميل-الملفات)
2. [تثبيت البرامج المطلوبة](#2-تثبيت-البرامج-المطلوبة)
3. [تشغيل الخادم محلياً](#3-تشغيل-الخادم-محليا)
4. [رفع على GitHub](#4-رفع-على-github)
5. [النشر على الإنترنت](#5-النشر-على-الإنترنت)
6. [استخدام الـ API](#6-استخدام-الـ-api)

---

# 1️⃣ تحميل الملفات

## الخطوة 1.1: تحميل ملف ZIP

**ستجدين ملف `tawal_backend.zip` يحتوي على:**
- `server.js` - الخادم الرئيسي
- `package.json` - قائمة المكتبات
- `package-lock.json` - إصدارات المكتبات
- `.gitignore` - ملفات عدم الرفع على GitHub
- `README_AR.md` - شرح كامل
- `QUICK_START_AR.md` - البدء السريع

## الخطوة 1.2: فك الضغط

### على Windows:
1. اضغطي كليك يمين على `tawal_backend.zip`
2. اختاري `Extract All...`
3. اختاري المجلد الذي تريدين حفظ الملفات فيه
4. اضغطي `Extract`

### على Mac:
1. اضغطي مرتين على `tawal_backend.zip`
2. سيتم فك الضغط تلقائياً

### على Linux:
```bash
unzip tawal_backend.zip
```

## الخطوة 1.3: فتح Terminal/Command Prompt

### على Windows:
1. اضغطي على `Windows + R`
2. اكتبي `cmd`
3. اضغطي `Enter`

### على Mac:
1. اضغطي على `Command + Space`
2. اكتبي `Terminal`
3. اضغطي `Enter`

### على Linux:
اضغطي `Ctrl + Alt + T`

## الخطوة 1.4: الذهاب إلى مجلد المشروع

في Terminal، اكتبي:
```bash
cd path/to/tawal_backend
```

**مثال:**
```bash
# على Windows
cd C:\Users\YourName\Downloads\tawal_backend

# على Mac/Linux
cd ~/Downloads/tawal_backend
```

---

# 2️⃣ تثبيت البرامج المطلوبة

## الخطوة 2.1: تثبيت Node.js

### على Windows:
1. اذهبي إلى: https://nodejs.org/
2. اضغطي على الزر الأخضر الكبير (LTS)
3. اضغطي `Download`
4. افتحي الملف المحمل
5. اتبعي التعليمات (اضغطي Next عدة مرات)
6. اضغطي `Finish`

### على Mac:
1. اذهبي إلى: https://nodejs.org/
2. اضغطي على الزر الأخضر الكبير (LTS)
3. اختاري `macOS Installer`
4. افتحي الملف المحمل
5. اتبعي التعليمات

### على Linux:
```bash
sudo apt update
sudo apt install nodejs npm
```

## الخطوة 2.2: التحقق من التثبيت

في Terminal، اكتبي:
```bash
node --version
npm --version
```

يجب أن تظهر أرقام الإصدارات:
```
v18.17.0
9.6.7
```

## الخطوة 2.3: تثبيت المكتبات

في مجلد المشروع، اكتبي:
```bash
npm install
```

**سيستغرق دقيقة أو دقيقتين**

ستظهر رسالة مثل:
```
added 183 packages in 2m
```

---

# 3️⃣ تشغيل الخادم محلياً

## الخطوة 3.1: بدء الخادم

في مجلد المشروع، اكتبي:
```bash
npm start
```

## الخطوة 3.2: التحقق من التشغيل

ستظهر رسالة مثل:
```
✓ الخادم يعمل على: http://localhost:3001
✓ API متاح على: http://localhost:3001/api

📚 الـ Endpoints المتاحة:
  POST   /api/students/register
  GET    /api/students/:id
  ...
```

## الخطوة 3.3: اختبار الخادم

افتحي المتصفح وادخلي:
```
http://localhost:3001/api/health
```

يجب أن تظهر:
```json
{
  "status": "OK",
  "message": "الخادم يعمل بشكل صحيح"
}
```

## الخطوة 3.4: إيقاف الخادم

في Terminal، اضغطي:
```
Ctrl + C
```

---

# 4️⃣ رفع على GitHub

## الخطوة 4.1: إنشاء حساب GitHub

1. اذهبي إلى: https://github.com/signup
2. أدخلي:
   - **Username**: اسم المستخدم (مثل: fatima_teacher)
   - **Email**: بريدك الإلكتروني
   - **Password**: كلمة مرور قوية
3. اضغطي `Create account`
4. أكملي التحقق من البريد الإلكتروني

## الخطوة 4.2: تثبيت Git

### على Windows:
1. اذهبي إلى: https://git-scm.com/download/win
2. اضغطي `Download`
3. افتحي الملف المحمل
4. اتبعي التعليمات (اضغطي Next عدة مرات)
5. اضغطي `Finish`

### على Mac:
```bash
# تثبيت Homebrew أولاً (إذا لم يكن مثبتاً)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# ثم تثبيت Git
brew install git
```

### على Linux:
```bash
sudo apt install git
```

## الخطوة 4.3: تكوين Git

في Terminal، اكتبي:
```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

**استبدلي الأسماء والبريد الإلكتروني بالفعلية**

## الخطوة 4.4: إنشاء مستودع على GitHub

1. اذهبي إلى: https://github.com/new
2. أدخلي:
   - **Repository name**: `tawal-academy-backend`
   - **Description**: `Backend API for Tawal Academy`
   - اختاري `Public`
3. اضغطي `Create repository`

## الخطوة 4.5: رفع الملفات

في Terminal (في مجلد المشروع)، اكتبي بالترتيب:

```bash
# خطوة 1: تهيئة Git
git init

# خطوة 2: إضافة جميع الملفات
git add .

# خطوة 3: حفظ التغييرات
git commit -m "Initial commit - Tawal Academy Backend"

# خطوة 4: تغيير اسم الفرع الرئيسي
git branch -M main

# خطوة 5: إضافة رابط المستودع
git remote add origin https://github.com/YOUR_USERNAME/tawal-academy-backend.git

# خطوة 6: رفع الملفات
git push -u origin main
```

**استبدلي `YOUR_USERNAME` باسم حسابك على GitHub**

## الخطوة 4.6: التحقق

1. اذهبي إلى: https://github.com/YOUR_USERNAME/tawal-academy-backend
2. يجب أن تظهر جميع الملفات

---

# 5️⃣ النشر على الإنترنت

## الخيار الأول: Heroku (مجاني)

### الخطوة 5.1.1: إنشاء حساب Heroku

1. اذهبي إلى: https://www.heroku.com/
2. اضغطي `Sign up`
3. أدخلي بياناتك
4. اضغطي `Create free account`
5. تحققي من بريدك الإلكتروني

### الخطوة 5.1.2: تثبيت Heroku CLI

#### على Windows:
1. اذهبي إلى: https://devcenter.heroku.com/articles/heroku-cli
2. اضغطي على `Download for Windows`
3. افتحي الملف المحمل
4. اتبعي التعليمات

#### على Mac:
```bash
brew tap heroku/brew && brew install heroku
```

#### على Linux:
```bash
curl https://cli-assets.heroku.com/install.sh | sh
```

### الخطوة 5.1.3: تسجيل الدخول إلى Heroku

في Terminal، اكتبي:
```bash
heroku login
```

سيفتح المتصفح، اضغطي `Log in`

### الخطوة 5.1.4: إنشاء تطبيق على Heroku

```bash
heroku create tawal-academy-backend
```

### الخطوة 5.1.5: رفع التطبيق

```bash
git push heroku main
```

### الخطوة 5.1.6: الرابط النهائي

```
https://tawal-academy-backend.herokuapp.com
```

---

## الخيار الثاني: Railway (الأسهل والأسرع)

### الخطوة 5.2.1: إنشاء حساب Railway

1. اذهبي إلى: https://railway.app
2. اضغطي `Sign up`
3. اختاري `GitHub`
4. أكملي التسجيل

### الخطوة 5.2.2: نشر التطبيق

1. اضغطي `New Project`
2. اختاري `Deploy from GitHub`
3. اختاري المستودع `tawal-academy-backend`
4. اضغطي `Deploy`

### الخطوة 5.2.3: الحصول على الرابط

1. اذهبي إلى `Settings`
2. اختاري `Domains`
3. سيظهر رابط مثل: `https://tawal-academy-backend.up.railway.app`

---

## الخيار الثالث: Render (بديل جيد)

### الخطوة 5.3.1: إنشاء حساب Render

1. اذهبي إلى: https://render.com
2. اضغطي `Sign up`
3. اختاري `GitHub`
4. أكملي التسجيل

### الخطوة 5.3.2: نشر التطبيق

1. اضغطي `New +`
2. اختاري `Web Service`
3. اختاري المستودع
4. أدخلي:
   - **Name**: `tawal-academy-backend`
   - **Start Command**: `npm start`
5. اضغطي `Create Web Service`

### الخطوة 5.3.3: الحصول على الرابط

سيظهر رابط مثل: `https://tawal-academy-backend.onrender.com`

---

# 6️⃣ استخدام الـ API

## الخطوة 6.1: تسجيل طالب جديد

**الطلب:**
```
POST http://localhost:3001/api/students/register
```

**في Postman أو curl:**
```bash
curl -X POST http://localhost:3001/api/students/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "أحمد محمد",
    "email": "ahmed@example.com"
  }'
```

**الرد:**
```json
{
  "id": 1,
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "message": "تم التسجيل بنجاح"
}
```

## الخطوة 6.2: حفظ نتيجة اختبار

**الطلب:**
```
POST http://localhost:3001/api/quiz-results
```

**في Postman أو curl:**
```bash
curl -X POST http://localhost:3001/api/quiz-results \
  -H "Content-Type: application/json" \
  -d '{
    "studentId": 1,
    "quizName": "GIS Networks",
    "score": 85,
    "totalQuestions": 10,
    "correctAnswers": 8
  }'
```

## الخطوة 6.3: جلب نتائج الطالب

**الطلب:**
```
GET http://localhost:3001/api/students/1/results
```

**في المتصفح:**
```
http://localhost:3001/api/students/1/results
```

## الخطوة 6.4: جلب إحصائيات الطالب

**الطلب:**
```
GET http://localhost:3001/api/students/1/stats
```

---

## 📝 ملخص الأوامر المهمة

| الأمر | الوصف |
|------|-------|
| `npm install` | تثبيت المكتبات |
| `npm start` | تشغيل الخادم |
| `git init` | تهيئة Git |
| `git add .` | إضافة الملفات |
| `git commit -m "message"` | حفظ التغييرات |
| `git push` | رفع على GitHub |
| `heroku create app-name` | إنشاء تطبيق على Heroku |
| `git push heroku main` | نشر على Heroku |

---

## ❓ أسئلة شائعة

**س: ماذا لو نسيت كلمة مرور GitHub؟**
ج: اذهبي إلى https://github.com/password_reset وأعيدي تعيينها

**س: كيف أحدّث الكود بعد النشر؟**
ج: عدّلي الملفات، ثم:
```bash
git add .
git commit -m "Update message"
git push
git push heroku main  # إذا كنتِ تستخدمين Heroku
```

**س: كيف أحذف مستودع من GitHub؟**
ج: اذهبي إلى Settings → Danger Zone → Delete this repository

**س: هل يمكن استخدام قاعدة بيانات أخرى؟**
ج: نعم، يمكن استبدال SQLite بـ MySQL أو PostgreSQL

---

## 🎉 تهانينا!

لقد أكملتِ جميع الخطوات بنجاح! 

الخادم الآن:
- ✅ يعمل محلياً على `http://localhost:3001`
- ✅ مرفوع على GitHub
- ✅ منشور على الإنترنت

---

**تم الإنشاء بواسطة**: Manus AI
**التاريخ**: 2025-11-08
